name: Devths-BE CI 파이프라인

# dev/release/main 모두 진행
on:
  pull_request:
    branches: [ develop, release, main ]

# ENV 설정
env:
  JAVA_VERSION: "21"

jobs:
  # Lint 설정
  lint:
    name: lint
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      checks: write
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: Java 및 Gradle 설치
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: gradle

      - name: Gradle 실행 권한 부여
        run: chmod +x gradlew

      - name: Lint (CheckStyle) 수행
        run: ./gradlew checkstyleMain

      - name: Lint (CheckStyle) 결과 리포트
        uses: lcollins/checkstyle-github-action@v2.0.0
        if: always()
        with:
          path: '**/build/reports/checkstyle/*.xml'
          title: 'Checkstyle 결과 Report'

  # CodeQL 기반 코드 정적 분석 실행
  static_analysis:
    name: static_analysis
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read
    strategy:
      fail-fast: true  # 정적 분석 실패 시 즉시 중단
      matrix:
        language: [ java-kotlin ]
    # container 부분은 제거 (GitHub 기본 환경 사용 권장)
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: Java 및 Gradle 설치
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}
          cache: 'gradle'

      - name: CodeQL 초기화
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          config-file: >-
            ${{ github.event_name == 'pull_request'
            && './.github/codeql/code-ql-light.yml'
            || './.github/codeql/code-ql-heavy.yml' }}

      - name: Build (Gradle)
        run: |
          chmod +x gradlew
          ./gradlew build -x test -x check

      - name: CodeQL 분석 수행
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{ matrix.language }}"
          upload: true

      - name: 보안 취약점 개수 확인 및 빌드 차단
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # 1. 변수 선언
          if [ "${{ github.event_name }}" == "pull_request" ]; then
            TARGET_REF="refs/pull/${{ github.event.pull_request.number }}/merge"
          else
            TARGET_REF="${{ github.ref }}"
          fi
          
          echo "📍 분석 대상 Ref: $TARGET_REF"
          echo "🔍 CodeQL 결과 반영 확인 중..."
          
          ALERTS_JSON=""
          # 2. Polling 로직 시작
          for i in {1..10}; do
            # 변수가 위에서 정의되었으므로 이제 정상 작동합니다.
            RESPONSE=$(gh api "repos/${{ github.repository }}/code-scanning/alerts?tool_name=CodeQL&state=open&ref=$TARGET_REF")
          
            # 응답이 비어있지 않은지 확인
            if [ -n "$RESPONSE" ] && [ "$RESPONSE" != "[]" ]; then
              ALERTS_JSON=$RESPONSE
              echo "✅ 데이터 반영 완료! ($((i*5))s 소요)"
              break
            fi
          
            # 취약점이 0개일 때 API가 []를 줄 수도 있으므로, 분석 결과 자체가 등록되었는지 확인이 필요
            # 하지만 안전하게 3~4번 정도는 더 찔러보는 것이 좋습니다.
            echo "⏳ 데이터 반영 대기 중... ($((i*5))s)"
            sleep 5
          done
          
          # 3. 데이터가 여전히 비어있다면 취약점이 0개인 것으로 간주
          if [ -z "$ALERTS_JSON" ] || [ "$ALERTS_JSON" == "[]" ]; then
            echo "✅ 보안 검사 통과: 발견된 취약점이 없습니다."
            exit 0
          fi
          
          # 4. 취약점 카운트 및 상세 출력
          ALERTS_COUNT=$(echo "$ALERTS_JSON" | jq -r "[.[] | select(.rule.security_severity_level != null)] | length")
          
          if [ "$ALERTS_COUNT" -gt 0 ]; then
            echo "⚠️ 발견된 보안 취약점: $ALERTS_COUNT개"
            echo "$ALERTS_JSON" | jq -r '.[] | select(.rule.security_severity_level != null) | "  - [\(.rule.security_severity_level)] \(.rule.description) \n    경로: \(.most_recent_instance.location.path):\(.most_recent_instance.location.start_line)"'
            echo "❌ 보안 검사 실패: 취약점을 해결하십시오."
            exit 1
          fi

  # 추후 통합 테스트 과정 추가 예정

  # 디스코드 알림 제공
  notify-discord:
    name: notify
    runs-on: ubuntu-22.04
    needs: [ lint, static_analysis ]
    if: always()
    steps:
      - name: 데이터 준비
        id: prep
        env:
          USER_MAP: ${{ secrets.USER_MAP_JSON }}
        run: |
          # 1. 작성자 매핑
          LOGIN_ID="${{ github.event.pull_request.user.login }}"
          AUTHOR_TAG=$(echo "$USER_MAP" | jq -r --arg id "$LOGIN_ID" '.[$id] // $id')
          echo "author=$AUTHOR_TAG" >> $GITHUB_OUTPUT

          # 2. 리뷰어 매핑 
          # jq를 사용하여 리뷰어 login 리스트를 가져온 뒤, MAP에서 찾아 디스코드 멘션으로 변환
          REVIEWERS_JSON='${{ toJson(github.event.pull_request.requested_reviewers) }}'
          REVIEWER_TAGS=$(echo "$REVIEWERS_JSON" | jq -r --argjson map "$USER_MAP" '
            [.[].login | ($map[.] // .)] | join(", ")
          ')
          echo "reviewers=${REVIEWER_TAGS:-없음}" >> $GITHUB_OUTPUT

          # 3. 작업 내용 추출 (HTML 주석 제거 로직 반영)
          PR_RAW_BODY=$(cat << 'EOF'
          ${{ github.event.pull_request.body }}
          EOF
          )
          
          # 필요없는 문구 삭제
          DESC=$(echo "$PR_RAW_BODY" | \
            perl -0777 -pe 's///gs' | \
            sed 's/이 PR에서 변경된 내용을 간략히 작성해주세요\.//g' | \
            sed -n '/## 📌 작업한 내용/,/## 🔍 참고 사항/p' | \
            sed '1d;$d' | \
            xargs | \
            head -c 300)
          
          echo "description=${DESC:-내용 없음}" >> $GITHUB_OUTPUT

      - name: Discord 빌드 알림
        uses: sarisia/actions-status-discord@v1
        with:
          status: ${{ (needs.lint.result == 'success' && needs.static_analysis.result == 'success') && 'success' || 'failure' }}
          webhook: ${{ secrets.DISCORD_WEBHOOK_URL }}
          title: "${{ github.event.pull_request.base.ref }} PR [${{ (needs.lint.result == 'success' && needs.static_analysis.result == 'success') && '✅성공' || '❌실패' }}]"
          description: |
            **PR 제목**: ${{ github.event.pull_request.title }}
            **작성자**: ${{ steps.prep.outputs.author }}
            **리뷰어**: ${{ steps.prep.outputs.reviewers }}
            **작업 내용**: ${{ steps.prep.outputs.description }}...
            **경로**: `${{ github.event.pull_request.head.ref }}` → `${{ github.event.pull_request.base.ref }}`
            
            **📊 상세 결과**:
            - Lint: ${{ needs.lint.result == 'success' && '✅' || '❌ FAILED' }}
            - Static Analysis (CodeQL): ${{ needs.static_analysis.result == 'success' && '✅' || ' ❌' }}

            ${{ needs.static_analysis.result != 'success' && '⚠️ **정적 분석 실패로 인해 파이프라인이 중단되었습니다.**' || '' }}

            **🔗 링크**: [PR 확인하기](${{ github.event.pull_request.html_url }})
          color: "${{ (needs.lint.result == 'success' && needs.static_analysis.result == 'success') && 65280 || 16711680 }}"
          username: GitHub PR Bot
          avatar_url: https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png
