name: Devths-BE CI 파이프라인

# dev/release/main 모두 진행
on:
  pull_request:
    branches: [ develop, release, main ]

# ENV 설정
env:
  JAVA_VERSION: "21"
  AWS_REGION: ${{ secrets.AWS_REGION }}
  S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
  S3_PREFIX: BE/${{ github.event.pull_request.base.ref }}
  CODEDEPLOY_APPLICATION: ${{ secrets.CODEDEPLOY_APPLICATION }}
  CODEDEPLOY_DEPLOYMENT_GROUP: >-
    ${{ github.event.pull_request.base.ref == 'main' && secrets.CODE_DEPLOY_GROUP_NAME_PROD ||
        github.event.pull_request.base.ref == 'release' && secrets.CODE_DEPLOY_GROUP_NAME_STAGING ||
        secrets.CODE_DEPLOY_GROUP_NAME_DEV
    }}

jobs:
  # Lint 설정
  lint:
    name: lint
    runs-on: ubuntu-22.04
    permissions:
      contents: read
      checks: write
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: Java 및 Gradle 설치
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: ${{ env.JAVA_VERSION }}
          cache: gradle

      - name: Gradle 실행 권한 부여
        run: chmod +x gradlew

      - name: Lint (CheckStyle) 수행
        run: ./gradlew checkstyleMain

      - name: Lint (CheckStyle) 결과 리포트
        uses: lcollins/checkstyle-github-action@v2.0.0
        if: always()
        with:
          path: '**/build/reports/checkstyle/*.xml'
          title: 'Checkstyle 결과 Report'

  # CodeQL 기반 코드 정적 분석 실행
  static_analysis:
    name: static_analysis
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      actions: read
      contents: read
    strategy:
      fail-fast: true  # 정적 분석 실패 시 즉시 중단
      matrix:
        language: [ java-kotlin ]
    # container 부분은 제거 (GitHub 기본 환경 사용 권장)
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: Java 및 Gradle 설치
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: ${{ env.JAVA_VERSION }}
          cache: 'gradle'

      - name: CodeQL 초기화
        uses: github/codeql-action/init@v4
        with:
          languages: ${{ matrix.language }}
          config-file: "./.github/codeql/code-ql-light.yml"

      - name: Build (Gradle)
        run: |
          chmod +x gradlew
          ./gradlew build -x test -x check

      - name: CodeQL 분석 수행
        uses: github/codeql-action/analyze@v4
        with:
          category: "/language:${{ matrix.language }}"
          upload: true

      - name: 보안 취약점 결과 확인 및 차단
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          echo "⏳ 분석 결과가 GitHub에 반영되기를 기다리는 중 (10초)..."
          sleep 10

          # 1. 현재 이벤트가 push인지 pr인지에 따라 검색할 REF 설정
          # GITHUB_REF는 push일 땐 refs/heads/branch명, PR일 땐 refs/pull/번호/merge가 됩니다.
          CURRENT_REF="${{ github.ref }}"
          echo "📍 현재 Ref: $CURRENT_REF"

          # 2. API 호출하여 High/Critical 등급의 Open 상태인 알림 조회
          ALERTS=$(gh api "repos/${{ github.repository }}/code-scanning/alerts?tool_name=CodeQL&state=open&ref=$CURRENT_REF" \
            --jq '[.[] | select(.rule.security_severity_level == "high" or .rule.security_severity_level == "critical")]')

          COUNT=$(echo "$ALERTS" | jq 'length')

          if [ "$COUNT" -gt 0 ]; then
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "❌ 보안 검사 실패: 고위험(High/Critical) 취약점 $COUNT개 발견"
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            echo "$ALERTS" | jq -r '.[] | "- [\(.rule.security_severity_level)] \(.rule.description) \n   위치: \(.most_recent_instance.location.path)"'
            echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
            exit 1
          else
            echo "✅ 보안 검사 통과: 현재 커밋($CURRENT_REF)은 안전합니다."
          fi

      - name: 빌드 아티팩트 저장
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: build/libs/*.jar
          retention-days: 1

  # 추후 통합 테스트 과정 추가 예정

  # 빌드
  build:
    name: build
    runs-on: ubuntu-22.04
    needs: [ lint, static_analysis ]
    if: success()
    outputs:
      package_name: ${{ steps.package_name.outputs.package_name }}
    steps:
      - name: 체크아웃
        uses: actions/checkout@v4

      - name: 빌드 아티팩트 다운로드
        uses: actions/download-artifact@v4
        with:
          name: build-artifacts
          path: build/libs

      - name: AWS 자격 증명 설정
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{secrets.AWS_ACCESS_KEY}}
          aws-secret-access-key: ${{secrets.AWS_SECRET_KEY}}
          aws-region: ${{secrets.AWS_REGION}}

      - name: 배포 패키지 이름 생성
        id: timestamp
        run: |
          # SHA의 앞 7자리만 변수에 저장
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          # 날짜와 SHORT_SHA를 결합하여 OUTPUT에 저장
          echo "value=$(date +'%Y%m%d_%H%M%S')-$SHORT_SHA" >> $GITHUB_OUTPUT

      - name: 배포 패키지 준비 및 S3 업로드
        run: |
          set -euo pipefail

          PACKAGE_NAME="${{ steps.timestamp.outputs.value }}"
          DEST="s3://${S3_BUCKET}/${S3_PREFIX}"
          DEPLOY_DIR="deploy"

          echo "📦 배포 패키지를 준비합니다..."

          # 1. 배포 디렉토리 생성
          mkdir -p $DEPLOY_DIR

          # 2. JAR 파일 복사 (plain 제외한 실행 파일만)
          ORIGINAL_JAR=$(ls build/libs/*.jar | grep -v 'plain' | head -n 1)

          if [ -f "$ORIGINAL_JAR" ]; then
            echo "📝 JAR 파일 복사: $ORIGINAL_JAR -> ${PACKAGE_NAME}.jar"
            cp "$ORIGINAL_JAR" "$DEPLOY_DIR/${PACKAGE_NAME}.jar"
          else
            echo "❌ JAR 파일을 찾을 수 없습니다."
            exit 1
          fi

          # 3. appspec.yml 복사
          if [ -f "appspec.yml" ]; then
            echo "📝 appspec.yml 복사"
            cp appspec.yml $DEPLOY_DIR/
          else
            echo "❌ appspec.yml 파일을 찾을 수 없습니다."
            exit 1
          fi

          # 4. 배포 스크립트 복사
          if [ -d "scripts" ]; then
            echo "📝 배포 스크립트 복사"
            cp -r scripts $DEPLOY_DIR/
          else
            echo "❌ scripts 디렉토리를 찾을 수 없습니다."
            exit 1
          fi

          # 5. 배포 환경 설정 파일 생성
          echo "🔧 배포 환경 설정 파일 생성"
          cat > $DEPLOY_DIR/deploy_env.sh <<EOF
          # 배포 환경 설정
          export BRANCH_NAME="${{ github.event.pull_request.base.ref }}"
          export SPRING_PROFILE="${{ github.event.pull_request.base.ref == 'main' && 'prod' || (github.event.pull_request.base.ref == 'release' && 'staging' || 'dev') }}"
          export PARAMETER_STORE_PATH="${{ github.event.pull_request.base.ref == 'main' && '/Prod/BE' || (github.event.pull_request.base.ref == 'release' && '/Staging/BE' || '/Dev/BE') }}"
          EOF

          # 6. ZIP 패키지 생성
          echo "🗜️  ZIP 패키지 생성: ${PACKAGE_NAME}.zip"
          cd $DEPLOY_DIR
          zip -r ../${PACKAGE_NAME}.zip .
          cd ..

          # 7. S3 업로드
          echo "🚀 S3에 업로드: ${PACKAGE_NAME}.zip"
          aws s3 cp ${PACKAGE_NAME}.zip ${DEST}/${PACKAGE_NAME}.zip --only-show-errors

          echo "✅ 배포 패키지 업로드 완료"

      - name: 배포 패키지 이름을 환경 변수로 저장
        id: package_name
        run: |
          PACKAGE_NAME="${{ steps.timestamp.outputs.value }}.zip"
          echo "package_name=$PACKAGE_NAME" >> $GITHUB_OUTPUT

  # CodeDeploy 기반 배포
  deploy:
    name: deploy
    runs-on: ubuntu-22.04
    needs: build
    if: success()
    outputs:
      deployment_id: ${{ steps.create_deployment.outputs.deployment_id }}
    steps:
      - name: AWS 자격 증명 설정
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{secrets.AWS_ACCESS_KEY}}
          aws-secret-access-key: ${{secrets.AWS_SECRET_KEY}}
          aws-region: ${{secrets.AWS_REGION}}

      - name: CodeDeploy 배포 생성
        id: create_deployment
        run: |
          # build job에서 업로드한 배포 패키지 정보
          PACKAGE_NAME="${{ needs.build.outputs.package_name }}"
          S3_LOCATION="s3://${S3_BUCKET}/${S3_PREFIX}/${PACKAGE_NAME}"

          echo "🚀 배포를 시작합니다..."
          echo "  애플리케이션: ${CODEDEPLOY_APPLICATION}"
          echo "  배포 그룹: ${CODEDEPLOY_DEPLOYMENT_GROUP}"
          echo "  S3 위치: ${S3_LOCATION}"
          echo "  배포 패키지: ${PACKAGE_NAME}"

          # CodeDeploy 배포 생성
          DEPLOYMENT_ID=$(aws deploy create-deployment \
            --application-name "${CODEDEPLOY_APPLICATION}" \
            --deployment-group-name "${CODEDEPLOY_DEPLOYMENT_GROUP}" \
            --s3-location bucket="${S3_BUCKET}",key="${S3_PREFIX}/${PACKAGE_NAME}",bundleType=zip \
            --description "GitHub Actions PR 배포 - PR #${{ github.event.pull_request.number }} - Commit: ${{ github.sha }}" \
            --query 'deploymentId' \
            --output text)

          echo "deployment_id=$DEPLOYMENT_ID" >> $GITHUB_OUTPUT
          echo "✅ 배포 ID: $DEPLOYMENT_ID"

      - name: CodeDeploy 배포 상태 대기 및 확인
        run: |
          DEPLOYMENT_ID="${{ steps.create_deployment.outputs.deployment_id }}"
          echo "⏳ 배포 완료를 기다리는 중... (배포 ID: $DEPLOYMENT_ID)"
          echo "   - EC2에서 블루그린 배포 및 헬스체크가 진행됩니다"
          echo "   - appspec.yml의 hooks에 정의된 스크립트가 순차적으로 실행됩니다"

          # 배포 완료 대기 (최대 20분)
          aws deploy wait deployment-successful \
            --deployment-id "$DEPLOYMENT_ID" \
            --cli-read-timeout 1200 \
            --cli-connect-timeout 60

          echo "✅ 배포가 성공적으로 완료되었습니다!"
          echo "   - 블루그린 배포: 완료"
          echo "   - 헬스체크 (Actuator): 통과"
          echo "   - Nginx 포트 스위칭: 완료"

  # 디스코드 알림 제공
  notify-discord:
    name: notify
    runs-on: ubuntu-22.04
    needs: [ lint, static_analysis, build, deploy ]
    if: always()
    steps:
      - name: 데이터 준비
        id: prep
        env:
          USER_MAP: ${{ secrets.USER_MAP_JSON }}
        run: |
          # 1. 작성자 매핑
          LOGIN_ID="${{ github.event.pull_request.user.login }}"
          AUTHOR_TAG=$(echo "$USER_MAP" | jq -r --arg id "$LOGIN_ID" '.[$id] // $id')
          echo "author=$AUTHOR_TAG" >> $GITHUB_OUTPUT

          # 2. 리뷰어 매핑 
          # jq를 사용하여 리뷰어 login 리스트를 가져온 뒤, MAP에서 찾아 디스코드 멘션으로 변환
          REVIEWERS_JSON='${{ toJson(github.event.pull_request.requested_reviewers) }}'
          REVIEWER_TAGS=$(echo "$REVIEWERS_JSON" | jq -r --argjson map "$USER_MAP" '
            [.[].login | ($map[.] // .)] | join(", ")
          ')
          echo "reviewers=${REVIEWER_TAGS:-없음}" >> $GITHUB_OUTPUT

          # 3. 작업 내용 추출 (HTML 주석 제거 로직 반영)
          PR_RAW_BODY=$(cat << 'EOF'
          ${{ github.event.pull_request.body }}
          EOF
          )
          
          # 필요없는 문구 삭제
          DESC=$(echo "$PR_RAW_BODY" | \
            perl -0777 -pe 's///gs' | \
            sed 's/이 PR에서 변경된 내용을 간략히 작성해주세요\.//g' | \
            sed -n '/## 📌 작업한 내용/,/## 🔍 참고 사항/p' | \
            sed '1d;$d' | \
            xargs | \
            head -c 300)
          
          echo "description=${DESC:-내용 없음}" >> $GITHUB_OUTPUT

      - name: Discord PR 알림
        uses: sarisia/actions-status-discord@v1
        with:
          status: ${{ (needs.lint.result == 'success' && needs.static_analysis.result == 'success' && needs.build.result == 'success' && needs.deploy.result == 'success') && 'success' || 'failure' }}
          webhook: ${{ secrets.DISCORD_WEBHOOK_URL }}
          title: "${{ github.event.pull_request.base.ref }} PR [${{ (needs.lint.result == 'success' && needs.static_analysis.result == 'success' && needs.build.result == 'success' && needs.deploy.result == 'success') && '✅성공' || '❌실패' }}]"
          description: |
            **PR 제목**: ${{ github.event.pull_request.title }}
            **작성자**: ${{ steps.prep.outputs.author }}
            **리뷰어**: ${{ steps.prep.outputs.reviewers }}
            **작업 내용**: ${{ steps.prep.outputs.description }}...
            **경로**: `${{ github.event.pull_request.head.ref }}` → `${{ github.event.pull_request.base.ref }}`
            **환경**: ${{ github.event.pull_request.base.ref == 'main' && '🚀 운영(PROD)' || (github.event.pull_request.base.ref == 'release' && '🧪 스테이징(STAGING)' || '🛠️ 개발(DEV)') }}

            **📊 상세 결과**:
            - Lint: ${{ needs.lint.result == 'success' && '✅' || '❌' }}
            - Static Analysis: ${{ needs.static_analysis.result == 'success' && '✅' || '❌' }}
            - Build: ${{ needs.build.result == 'success' && '✅' || (needs.build.result == 'skipped' && '⏭️' || '❌') }}
            - Deploy (CodeDeploy + Health Check): ${{ needs.deploy.result == 'success' && '✅' || (needs.deploy.result == 'skipped' && '⏭️' || '❌') }}

            ${{ needs.static_analysis.result != 'success' && '⚠️ **정적 분석 실패로 인해 빌드가 중단되었습니다.**' || '' }}
            ${{ needs.deploy.result == 'success' && '🎉 **블루그린 배포 및 헬스체크가 완료되었습니다!**' || '' }}
            ${{ needs.deploy.result == 'failure' && '❌ **배포 또는 헬스체크가 실패했습니다.**' || '' }}

            **🔗 링크**: [PR 확인하기](${{ github.event.pull_request.html_url }})
          color: "${{ (needs.lint.result == 'success' && needs.static_analysis.result == 'success' && needs.build.result == 'success' && needs.deploy.result == 'success') && 65280 || 16711680 }}"
          username: GitHub PR Bot
          avatar_url: https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png
